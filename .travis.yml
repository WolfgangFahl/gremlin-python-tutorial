# see https://docs.travis-ci.com/user/languages/python/
language: python
# make sure gremlin server is installed and started
before_script:
  - ./run -i
  - ./run -s&
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest
